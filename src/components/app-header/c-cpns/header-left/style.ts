import { styled } from 'styled-components'

export const LeftWrapper = styled.div`
  display: flex;
  .logo {
    padding: 0 20px;
    font-size: 18px;
    font-weight: 700;
    color: var(--theme-color);
  }
`
