import { styled } from 'styled-components'

export const BaseStyleWrapper = styled.div`
  .headline {
    cursor: pointer;

    .top {
      display: flex;
      align-items: center;
      margin-bottom: 4px;
    }
  }
`
