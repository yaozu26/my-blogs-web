import { styled } from 'styled-components'

export const UserWrapper = styled.div`
  display: flex;

  .right {
    width: 100%;
    .top {
      height: 50px;
      background-color: #fff;
    }
  }
`
